import { Version } from "../API/fields/other/Version";

export abstract class Versions {
	public static readonly REIGN_OF_CHAOS = new Version(0)
	public static readonly FROZEN_THRONE = new Version(1)
}